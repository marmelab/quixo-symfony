{% extends 'base.html.twig' %}
{% block content %}
    <div class="board">
        {% for line in board|default([]) %}
            {% for tile in line|default([]) %}
                <div class="cube cube-{{tile.value|getSymbol}}">
                </div>
            {% endfor %}
            <div></div>
        {% endfor %}
    </div>
{% endblock %}
